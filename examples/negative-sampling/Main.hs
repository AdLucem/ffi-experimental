

-- consider a pair (w, c) of word and context
data Pair = (String, String)

-- consider the training data as a list of size 'n' windows
-- with each word being in the center of it's context window
data TrainingData = [[String]]

-- probability that pair in training data, given parameters theta
-- for a distribution
p theta = 

main :: IO ()
main = putStrLn "hello"